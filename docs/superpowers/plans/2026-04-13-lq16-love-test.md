# LQ16 恋爱人格矩阵 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a new LQ16 love personality test alongside the existing SBTI test, sharing the React component layer and API infrastructure.

**Architecture:** Create a `TestConfig` React Context that provides test-specific data (dimensions, questions, types, compatibility) to all shared components. The existing SBTI app wraps with SBTI config, and the new love test wraps with love config. Both share the same component tree. Utils and hooks are generalized to accept config parameters. The API layer adds a `test` parameter to namespace Redis keys.

**Tech Stack:** React 18, TypeScript, Vite, Tailwind CSS, Framer Motion, Upstash Redis, Vercel Serverless Functions

**Spec:** `docs/superpowers/specs/2026-04-13-lq16-love-personality-test.md`

---

## File Structure

### New files
| File | Purpose |
|------|---------|
| `src/data/testConfig.ts` | TestConfig interface + React Context + Provider |
| `src/data/love/dimensions.ts` | 4 love dimensions + explanations |
| `src/data/love/questions.ts` | 16 questions + 1 hidden trigger |
| `src/data/love/types.ts` | 16 types + EX hidden + HWDP fallback |
| `src/data/love/typeImages.ts` | Placeholder image paths |
| `src/data/love/compatibility.ts` | 16 soulmate/rival pairings |
| `src/data/love/config.ts` | Love TestConfig instance |
| `src/data/sbti/config.ts` | SBTI TestConfig instance (wraps existing data) |
| `src/LoveApp.tsx` | Love test entry point (parallel to App.tsx) |
| `love.html` | Vite entry HTML for love test |

### Modified files
| File | Change |
|------|--------|
| `src/utils/matching.ts` | Accept config params instead of importing data |
| `src/utils/quiz.ts` | Accept questions as params |
| `src/utils/compare.ts` | Accept dimensionOrder as param |
| `src/hooks/useQuiz.ts` | Read config from context |
| `src/hooks/useRanking.ts` | Accept test param for API path |
| `src/hooks/useLocalHistory.ts` | Accept test param + localStorage keys |
| `src/components/DimList.tsx` | Read dimension data from context |
| `src/components/ResultPage.tsx` | Read type/compat data from context |
| `src/components/ComparePage.tsx` | Read type/dim data from context |
| `src/App.tsx` | Wrap with SBTI config provider |
| `api/record.js` | Accept `test` body param for Redis key prefix |
| `api/ranking.js` | Accept `test` query param for Redis key prefix |
| `vite.config.ts` | Add love.html as second entry |
| `build.sh` | Copy love build output to dist/new/love/ |

---

### Task 1: Create TestConfig interface + React Context

**Files:**
- Create: `src/data/testConfig.ts`

- [ ] **Step 1: Create the TestConfig type and context**

```typescript
// src/data/testConfig.ts
import { createContext, useContext, type ReactNode } from 'react';

export interface DimensionInfo {
  name: string;
  model: string;
}

export interface QuestionOption {
  label: string;
  value: number;
}

export interface Question {
  id: string;
  dim?: string;
  text: string;
  options: QuestionOption[];
  special?: boolean;
  kind?: string;
}

export interface TypeDef {
  code: string;
  cn: string;
  intro: string;
  desc: string;
}

export interface NormalType {
  code: string;
  pattern: string;
}

export interface RarityInfo {
  pct: number;
  stars: number;
  label: string;
}

export interface CompatEntry {
  type: 'soulmate' | 'rival';
  say: string;
}

export interface CompatResult {
  type: 'soulmate' | 'rival' | 'mirror' | 'normal';
  say: string;
}

export interface TestConfig {
  id: string;
  name: string;

  // Dimensions
  dimensionOrder: string[];
  dimensionMeta: Record<string, DimensionInfo>;
  dimExplanations: Record<string, Record<string, string>>;

  // Questions
  questions: Question[];
  specialQuestions: Question[];
  gateQuestionId: string;
  gateAnswerValue: number;
  hiddenTriggerQuestionId: string;
  hiddenTriggerValue: number;

  // Types
  typeLibrary: Record<string, TypeDef>;
  normalTypes: NormalType[];
  typeRarity: Record<string, RarityInfo>;
  typeImages: Record<string, string>;
  shareImages: Record<string, string>;

  // Compatibility
  compatibility: Record<string, CompatEntry>;
  getCompatibility: (a: string, b: string) => CompatResult;

  // Matching params
  sumToLevel: (score: number) => string;
  maxDistance: number;
  fallbackTypeCode: string;
  hiddenTypeCode: string;
  similarityThreshold: number;

  // URLs & Storage
  prodBaseUrl: string;
  basePath: string;
  localHistoryKey: string;
  localStatsKey: string;
  apiTestParam: string;

  // Display text
  dimSectionTitle: string;
  questionCountLabel: string;
}

const TestConfigContext = createContext<TestConfig | null>(null);

export function TestConfigProvider({
  config,
  children,
}: {
  config: TestConfig;
  children: ReactNode;
}) {
  return (
    <TestConfigContext.Provider value={config}>
      {children}
    </TestConfigContext.Provider>
  );
}

export function useTestConfig(): TestConfig {
  const ctx = useContext(TestConfigContext);
  if (!ctx) throw new Error('useTestConfig must be used within TestConfigProvider');
  return ctx;
}
```

- [ ] **Step 2: Verify TypeScript compiles**

Run: `npx tsc --noEmit 2>&1 | head -20`
Expected: No errors related to testConfig.ts

- [ ] **Step 3: Commit**

```bash
git add src/data/testConfig.ts
git commit -m "feat(lq16): add TestConfig interface and React context"
```

---

### Task 2: Create love test data files

**Files:**
- Create: `src/data/love/dimensions.ts`
- Create: `src/data/love/questions.ts`
- Create: `src/data/love/types.ts`
- Create: `src/data/love/typeImages.ts`
- Create: `src/data/love/compatibility.ts`

- [ ] **Step 1: Create love dimensions**

```typescript
// src/data/love/dimensions.ts
import type { DimensionInfo } from '../testConfig';

export const dimensionMeta: Record<string, DimensionInfo> = {
  D1: { name: '恋爱驱动', model: '驱动模型' },
  D2: { name: '恋爱姿态', model: '姿态模型' },
  D3: { name: '亲密模式', model: '亲密模型' },
  D4: { name: '爱情信念', model: '信念模型' },
};

export const dimensionOrder = ['D1', 'D2', 'D3', 'D4'];

export const DIM_EXPLANATIONS: Record<string, Record<string, string>> = {
  D1: {
    A: '你谈恋爱全凭一个字——感觉。条件再好没心动等于零，感觉对了住地下室都行。你相信荷尔蒙不会骗人，但你忘了荷尔蒙的保质期只有三个月。',
    B: '你的恋爱观本质上是一份投资分析报告。对方的学历、收入、家庭背景、未来发展潜力，你全部量化评估完毕才决定要不要投入感情。你不是不浪漫，你只是觉得浪漫解决不了房贷。',
  },
  D2: {
    A: '看上了就冲，被拒了就再冲。你信奉"主动的人运气不会太差"，追人的时候整个人都在发光。你享受攻城略地的快感，哪怕满身伤痕也觉得值。',
    B: '你坚信"该来的会来"。主动追人对你来说比裸奔还难为情。你擅长释放微弱信号然后疯狂暗示，最后对方没get到你就告诉自己"果然不是对的人"。',
  },
  D3: {
    A: '谈恋爱就要像连体婴儿一样——共享定位、共享密码、共享朋友圈、共享人生。你觉得真正的爱就是没有秘密，对方要是不让你看手机那一定有鬼。',
    B: '谈恋爱归谈恋爱，你的私人空间神圣不可侵犯。共享密码？不存在的。你连周末要不要见面都需要提前预约。你不是不爱，你只是觉得两个独立的灵魂比一坨黏在一起的糨糊高级。',
  },
  D4: {
    A: '你是爱情童话的原教旨主义者。你相信月老、相信命运的安排、相信世界上存在那个跟你灵魂严丝合缝的人。偶像剧看哭你的不是演技，是你觉得"这就是我未来的人生"。',
    B: '你清醒得让人心疼。你知道爱情没有完美，婚姻本质是合伙制企业，激情终会被柴米油盐稀释。你不是不浪漫，你只是拒绝为了一个幻觉把自己搭进去。',
  },
};
```

- [ ] **Step 2: Create love questions**

```typescript
// src/data/love/questions.ts
import type { Question } from '../testConfig';

export const questions: Question[] = [
  // D1 恋爱驱动 (H心 vs B脑)
  {
    id: 'lq1', dim: 'D1',
    text: '有人给你介绍对象，你第一反应是？',
    options: [
      { label: '先看照片，有feel再说', value: 1 },
      { label: '照片条件都看看，综合判断', value: 2 },
      { label: '先问职业学历收入，数据合格再见', value: 3 },
    ],
  },
  {
    id: 'lq2', dim: 'D1',
    text: '你对"门当户对"怎么看？',
    options: [
      { label: '爱情面前一切条件都是浮云', value: 1 },
      { label: '有道理但不是决定性的', value: 2 },
      { label: '不门当户对的关系迟早出问题', value: 3 },
    ],
  },
  {
    id: 'lq3', dim: 'D1',
    text: '朋友劝你"TA条件很好别错过"，但你就是没感觉。你会？',
    options: [
      { label: '没心动就是没心动，条件再好也白搭', value: 1 },
      { label: '犹豫一下，先接触看看能不能培养感觉', value: 2 },
      { label: '试试呗，感觉这东西可以慢慢来', value: 3 },
    ],
  },
  {
    id: 'lq4', dim: 'D1',
    text: '回忆你历史上的心动瞬间，让你上头的通常是？',
    options: [
      { label: '一个眼神、一句话、某个瞬间的氛围', value: 1 },
      { label: '相处久了发现对方各方面都不错', value: 2 },
      { label: '了解到对方的实力和人品后才真正动心', value: 3 },
    ],
  },
  // D2 恋爱姿态 (A攻 vs W守)
  {
    id: 'lq5', dim: 'D2',
    text: '你在聚会上看到一个让你心动的陌生人，你会？',
    options: [
      { label: '直接走过去搭话，机会稍纵即逝', value: 1 },
      { label: '找朋友帮忙引荐，给自己留点余地', value: 2 },
      { label: '偷看两眼，然后等TA来找你', value: 3 },
    ],
  },
  {
    id: 'lq6', dim: 'D2',
    text: '暧昧期你发了条消息对方两小时没回，你会？',
    options: [
      { label: '再发一条，不玩猜心游戏', value: 1 },
      { label: '等等看，但心里开始胡思乱想了', value: 2 },
      { label: '放下手机做自己的事，TA想回自然会回', value: 3 },
    ],
  },
  {
    id: 'lq7', dim: 'D2',
    text: '关于表白这件事，你的态度是？',
    options: [
      { label: '喜欢就说，被拒了也比不说好', value: 1 },
      { label: '等关系更近一些、确定对方也有意思再说', value: 2 },
      { label: '从不主动表白，如果对方也喜欢TA会来找我', value: 3 },
    ],
  },
  {
    id: 'lq8', dim: 'D2',
    text: '在感情中你更像？',
    options: [
      { label: '追光者——看到目标就冲', value: 1 },
      { label: '看情况，有时主动有时被动', value: 2 },
      { label: '磁铁——吸引对方来找我', value: 3 },
    ],
  },
  // D3 亲密模式 (M融 vs D界)
  {
    id: 'lq9', dim: 'D3',
    text: '你理想的情侣日常是？',
    options: [
      { label: '每天腻在一起，干什么都一起', value: 1 },
      { label: '有各自的空间，但每天要联系', value: 2 },
      { label: '各过各的，约了再见面', value: 3 },
    ],
  },
  {
    id: 'lq10', dim: 'D3',
    text: '对方想看你的手机，你的反应是？',
    options: [
      { label: '随便看，我没什么好藏的', value: 1 },
      { label: '可以看，但你这个行为让我有点不舒服', value: 2 },
      { label: '不行。手机是我的私人领地', value: 3 },
    ],
  },
  {
    id: 'lq11', dim: 'D3',
    text: '你们吵架冷战了一天，你会？',
    options: [
      { label: '受不了，立刻打电话求和', value: 1 },
      { label: '等到晚上，主动发条消息缓和一下', value: 2 },
      { label: '各自冷静，想通了再聊', value: 3 },
    ],
  },
  {
    id: 'lq12', dim: 'D3',
    text: '关于"交出所有社交账号密码"，你觉得？',
    options: [
      { label: '这是信任的表现，我愿意', value: 1 },
      { label: '没必要但如果对方需要可以给', value: 2 },
      { label: '绝对不行，爱情不等于放弃隐私', value: 3 },
    ],
  },
  // D4 爱情信念 (R梦 vs P实)
  {
    id: 'lq13', dim: 'D4',
    text: '你相信"命中注定的另一半"这回事吗？',
    options: [
      { label: '绝对相信，TA在某个地方等我', value: 1 },
      { label: '半信半疑，缘分和努力各占一半吧', value: 2 },
      { label: '不信。合适的人是选出来和磨合出来的', value: 3 },
    ],
  },
  {
    id: 'lq14', dim: 'D4',
    text: '恋爱三年，激情褪去了，你怎么看？',
    options: [
      { label: '说明不是真爱，真爱应该永远心动', value: 1 },
      { label: '正常，但要想办法保持新鲜感', value: 2 },
      { label: '太正常了，感情本来就会从激情变成亲情', value: 3 },
    ],
  },
  {
    id: 'lq15', dim: 'D4',
    text: '伴侣各方面都好，但你就是没有怦然心动的感觉。你会？',
    options: [
      { label: '没有心动的感觉我做不到，对不起', value: 1 },
      { label: '纠结很久，可能先处着看', value: 2 },
      { label: '心动不心动不重要，合适最重要', value: 3 },
    ],
  },
  {
    id: 'lq16', dim: 'D4',
    text: '你对"爱情片/恋爱小说"的态度是？',
    options: [
      { label: '超爱看！看完相信世界上真的有那样的爱情', value: 1 },
      { label: '偶尔看看当消遣，看完也就看完了', value: 2 },
      { label: '很少看，太不真实了看着尴尬', value: 3 },
    ],
  },
];

export const specialQuestions: Question[] = [
  {
    id: 'ex_gate',
    text: '你手机里还有前任的聊天记录吗？',
    options: [
      { label: '早删了，眼不见心不烦', value: 1 },
      { label: '还在但基本不看了', value: 2 },
      { label: '还在……偶尔会翻一翻', value: 3 },
    ],
    special: true,
  },
];

export const EX_TRIGGER_QUESTION_ID = 'ex_gate';
```

- [ ] **Step 3: Create love types**

Create `src/data/love/types.ts` with all 16 type definitions + EX hidden type + HWDP as fallback. Each type has code, cn, intro, desc. The full descriptions come from the design spec (`docs/superpowers/specs/2026-04-13-lq16-love-personality-test.md` section II).

```typescript
// src/data/love/types.ts
import type { TypeDef, NormalType, RarityInfo } from '../testConfig';

export const TYPE_LIBRARY: Record<string, TypeDef> = {
  BAMR: {
    code: 'BAMR',
    cn: '猎头',
    intro: 'The Headhunter',
    desc: '你找对象的方式和猎头找候选人一模一样——先定义岗位需求，再全网筛选简历，然后精准出击主动邀约。你的恋爱从来不是偶遇，而是一场策划周密的人才收购。你很清楚自己要什么，也很清楚怎么拿到。感情对你来说不是命运的馈赠，而是项目管理。\n\n天赋优势：目标感极强，看上的人很少跑掉；极度清醒，绝不会在不值得的人身上浪费时间；谈恋爱像打仗一样有策略，成功率极高。\n\n隐秘代价：你太把感情当项目了，对方觉得自己不是被爱而是被"管理"；当一切都在你掌控之中的时候，你反而觉得索然无味；你最害怕的事不是被拒绝，而是遇到一个让你所有计划失控的人。\n\n一句刺痛你的话：你赢下了每一局博弈，却没人愿意跟你玩第二轮。',
  },
  BAMP: {
    code: 'BAMP',
    cn: '操盘手',
    intro: 'The Dealer',
    desc: '你是感情场上的华尔街之狼。你不信命，只信赔率。你极度擅长在关系中创造不对等——你永远是付出更少、在乎更少、但掌控更多的那一个。你谈恋爱的底层逻辑是"我可以没有你，但你不能没有我"，这让你在每段感情中都立于不败之地，也让你永远尝不到真正交出自己的滋味。\n\n天赋优势：绝对不会在感情中卑微，你永远有筹码；读人极准，对方的弱点在你眼里一览无余；危机处理能力满分，什么场面都hold得住。\n\n隐秘代价：你把每段感情都变成了交易，最后连你自己都分不清哪部分是真的；对方爱上的是你精心设计的人设，不是你；某天深夜你突然想到：有没有一个人是真的因为你这个人而爱你？答案让你失眠。\n\n一句刺痛你的话：你操控了所有人的感情，唯独不敢面对自己的。',
  },
  BADR: {
    code: 'BADR',
    cn: '策展人',
    intro: 'The Curator',
    desc: '你对爱情有一套极其清晰的美学标准。你不是在找"合适的人"，你是在策展——对方必须在颜值、品味、谈吐、生活方式上全部达标，才有资格进入你的人生展厅。你主动、理性、有行动力，但你的主动是有门槛的主动，你只会对符合你审美体系的人出手。\n\n天赋优势：品味极佳，你的伴侣永远是朋友圈里最让人羡慕的；对关系质量有极高追求，绝不凑合；会给对方极精致的恋爱体验，约会品质拉满。\n\n隐秘代价：你爱的不是那个人，是那个人身上符合你标准的那部分；对方一旦"跌落神坛"（发胖、变懒、品味下降），你的爱会断崖式消失；你精心策展的完美关系，其实脆弱得像玻璃柜里的艺术品。\n\n一句刺痛你的话：你把爱人当艺术品来选，却忘了艺术品不会在你生病的时候给你倒杯热水。',
  },
  BADP: {
    code: 'BADP',
    cn: '审计官',
    intro: 'The Auditor',
    desc: '你是感情世界里最冷静的那种人——冷静到可怕。你谈恋爱之前会做一份完整的风险评估报告，包括对方的原生家庭、消费习惯、前任数量和分手原因。你不是不浪漫，你只是觉得浪漫是及格线以上的加分项，及格线是这个人在你的人生规划里不会成为负资产。\n\n天赋优势：绝对不会被渣，你的风控体系比银行还严；选择的伴侣稳定性极高，你的婚姻是概率最高的那种；极度靠谱，答应的事情绝对做到，是最让人安心的长期伴侣。\n\n隐秘代价：你把爱情的所有不确定性都消灭了，连心动的可能性也一起消灭了；对方觉得跟你谈恋爱像在通过安检，每一次亲密都要被扫描；你找到了最"正确"的人，但躺在旁边的时候你偶尔会想：如果当初选那个"错误"的人，会不会更开心？\n\n一句刺痛你的话：你规避了所有的风险，也规避了所有的心动。',
  },
  BWMR: {
    code: 'BWMR',
    cn: '完美主义者',
    intro: 'The Perfectionist',
    desc: '你脑子里住着一个完美伴侣的3D建模——身高体重三观谈吐甚至笑的时候露几颗牙都有标准。你不是不想恋爱，你是在等那个100%匹配的人从天上掉下来。你用理性设定了一个不可能达到的标准，然后用"缘分未到"当挡箭牌，优雅地逃避了所有可能的开始。\n\n天赋优势：标准清晰，绝对不会陷入烂感情；一个人的时候也能把生活经营得很精致；一旦真的遇到接近标准的人，会爆发出惊人的深情。\n\n隐秘代价：你的完美标准是一个不断升级的bug——你越优秀，标准越高，越找不到；你已经习惯了评判别人，却忘了自己也经不起同等标准的审视；你嘴上说"宁缺毋滥"，身体很诚实地在每个节假日多刷两小时短视频。\n\n一句刺痛你的话：你等的那个完美的人确实存在——在你的想象里。',
  },
  BWMP: {
    code: 'BWMP',
    cn: '局外人',
    intro: 'The Bystander',
    desc: '你看别人谈恋爱就像看纪录片——有趣、精彩、跟我无关。你不是不懂爱情，恰恰相反，你太懂了。你把爱情的底层逻辑拆解得清清楚楚：多巴胺、催产素、沉没成本、损失厌恶……当你用科学解构完爱情之后，你发现自己已经丧失了"不明所以就一头扎进去"的能力。\n\n天赋优势：看感情问题比当事人还清楚，是朋友圈里最好的情感分析师；完全不会被感情绑架，活得极其清醒自在；情绪极其稳定，永远不会因为爱情做出蠢事。\n\n隐秘代价：你分析完了所有人的感情，唯独不敢分析自己为什么一直一个人；"看透"变成了你最坚固的盔甲，也是你最深的牢笼；有时候你会突然羡慕那些哭着喊着说"我好爱TA"的傻子。\n\n一句刺痛你的话：你站在爱情的门外看了一辈子风景，门其实从来没锁过。',
  },
  BWDR: {
    code: 'BWDR',
    cn: '理想型收藏家',
    intro: 'The Collector',
    desc: '你的手机里一定有一个隐藏相册，收藏着各种让你心动的人的照片、截图、甚至语音。你不会主动去追任何一个人，但你会默默地把每一个让你心动过的瞬间收藏起来，像一个博物馆管理员。你在精神世界里拥有丰富的感情生活，但在现实中你是个彻底的旁观者。\n\n天赋优势：审美极佳，你欣赏人的眼光总是很准；内心世界极其丰富，不需要真正恋爱也能获得情感满足；不会伤害任何人，因为你连开始都不曾。\n\n隐秘代价：你收藏了一屋子的"心动"，但没有一个变成了"在一起"；你已经习惯了远远欣赏，近距离接触反而让你不知所措；多年后翻看收藏夹，你分不清那是回忆还是你从未拥有过的幻觉。\n\n一句刺痛你的话：你收藏了全世界的心动，自己却从来不在任何人的收藏夹里。',
  },
  BWDP: {
    code: 'BWDP',
    cn: '孤岛',
    intro: 'The Island',
    desc: '你把自己活成了一座有Wi-Fi的孤岛——物质上自给自足，精神上拒绝登陆。你嘴上说着"一个人也挺好"，但你的"挺好"不过是"懒得失望"的体面说法。你把标准定得高到离谱，不是因为你真的值那个价，而是因为标准越高，你需要面对的风险就越低。说到底，你不是在找灵魂伴侣，你是在找一个不存在的人，好让自己有理由继续躲着。\n\n天赋优势：情绪价值完全自产自销，不依赖任何人就能活得体面；看人极准，三秒钟就能闻出谁是装的；永远不会因为寂寞做出让自己后悔的决定。\n\n隐秘代价：你已经分不清"享受孤独"和"恐惧亲密"的区别了；有人真心靠近的时候你会本能地制造距离，然后告诉自己"果然不合适"；深夜三点醒来那一瞬间的空旷感，你谁都没告诉过。\n\n一句刺痛你的话：你不是不需要人陪，你只是提前把每一个可能伤你的人判了死刑。',
  },
  HAMR: {
    code: 'HAMR',
    cn: '飞蛾',
    intro: 'The Moth',
    desc: '说白了，你就是感情世界里的人肉ATM——感情的、时间的、尊严的，全部无条件提款。别人还在试探的时候，你已经all in了。你以为你是在"勇敢地爱"，其实你只是上瘾了。你对那种心脏被攥紧的感觉有病态的迷恋，哪怕明知道对方是个坑，你也要闭着眼往里跳，还觉得这叫浪漫。\n\n天赋优势：爱的浓度足以让任何人溺毙，你的真心是稀缺品；极强的情绪感染力，你认真爱的时候整个世界都在发光；别人做不到的不顾一切，对你来说只是出厂设置。\n\n隐秘代价：你不是在谈恋爱，你是在找人来验证自己值得被爱；每次失恋不是心碎，是整个操作系统崩溃需要重装；你的"无条件"其实是一种绑架——对方稍微后退一步，你就开始自我毁灭。\n\n一句刺痛你的话：你那么拼命地爱别人，不过是因为你从来没学会怎么爱自己。',
  },
  HAMP: {
    code: 'HAMP',
    cn: '纵火犯',
    intro: 'The Arsonist',
    desc: '你谈恋爱的方式跟纵火差不多——点完火欣赏一会儿火焰，然后在消防车来之前全身而退。你太懂怎么让一个人喜欢你了，那些所谓的"恋爱技巧"在你这里是肌肉记忆。你享受的不是爱情本身，而是那种"我能让你为我疯狂"的权力感。问题是，你把这种能力练得太熟了，以至于连你自己都不确定哪次是真的动了心。\n\n天赋优势：天生的人性阅读器，暧昧博弈的绝对王者；感情里永远有主动权，你离开谁都能活得漂亮；社交场上的核武器，走到哪里都是焦点。\n\n隐秘代价：你撩了一百个人，但没有一个人真正见过你卸妆的样子；当你终于想认真的那一天，你的历史记录会替你回答一切；你最恐惧的不是孤独，而是有一天发现自己已经丧失了真心爱人的能力。\n\n一句刺痛你的话：你以为你是玩家，其实你是那个最怕输的人——所以你从来不下注。',
  },
  HADR: {
    code: 'HADR',
    cn: '骑士',
    intro: 'The Knight',
    desc: '你是感情世界里最稀有的物种——明知道可能没有回报，还是义无反顾地出发了。你把爱当成一种信仰，不是因为对方值得，而是因为你觉得"全力以赴地爱一个人"这件事本身就是神圣的。你勇敢、炽烈、保持距离地守护，像中世纪的骑士一样。你不占有，你只奉献。问题是——这个时代已经没有城堡了。\n\n天赋优势：极致的纯粹感，你的爱能让最冷的人也被融化；超强的精神力量，失恋了也不会扭曲变形；你是那种让人多年后想起来会觉得"我错过了一个真正的好人"的人。\n\n隐秘代价：你的骑士精神在别人眼里经常约等于"舔狗"；你总是爱上不需要被拯救的人，然后在自我感动中独自收场；你嘴上说不求回报，但身体每一个细胞都在等对方回头。\n\n一句刺痛你的话：你为别人披荆斩棘了一路，到终点才发现那里等着的人不是你。',
  },
  HADP: {
    code: 'HADP',
    cn: '赌徒',
    intro: 'The Gambler',
    desc: '你谈恋爱的信条就四个字——及时行乐。你不想规划未来、不想定义关系、不想承诺永远。你喜欢一个人的时候会毫不犹豫地冲上去，但你的热情来得快去得也快。你把每段感情都当成一场赌局——赢了就多玩一会儿，输了就翻下一张牌。你活在当下，活得很燃，也活得很费人。\n\n天赋优势：恋爱体验值全服最高，你见过的风景别人一辈子都看不到；极度坦诚，不会给对方画大饼或者虚假承诺；活在当下的每一刻都是真心的，这种纯粹很罕见。\n\n隐秘代价：你的"真心"保质期太短，对方把你当终点你把对方当驿站；感情经历越丰富，你越难被打动，阈值越来越高；某天你会突然发现身边换了无数人，但没有一个真正留下过痕迹。\n\n一句刺痛你的话：你赌赢了无数场刺激，却赌不来一个愿意和你数柴米油盐的人。',
  },
  HWMR: {
    code: 'HWMR',
    cn: '月光信徒',
    intro: 'The Moongazer',
    desc: '你的恋爱史有99%发生在你的脑子里。你看到一个人的侧脸好看，就能在大脑里生成从初遇到白头的完整4K电影。现实中呢？你连句"在吗"都要打了删删了打十七遍。你坚信"对的人会来找你"，但你忘了你把自己藏得连GPS都定位不到。你不是没有爱人的能力，你只是把所有的热烈都关在了门里面，让它自己发酵到变质。\n\n天赋优势：你的情感浓度是核弹级的，一旦真的释放出来足以震撼任何人；忠诚度MAX，你认定了就是一辈子，不打折不退货；共情力极强，你能听懂对方连自己都说不出口的那种委屈。\n\n隐秘代价：你暗恋的次数比你吃过的泡面还多，而对方一次都不知道；你对爱情的想象太完美了，以至于每个真人都自动打七折；你最大的遗憾不是爱错了人，而是一个"喜欢你"卡在嗓子眼一辈子都没说出口。\n\n一句刺痛你的话：你在月光下等了一辈子的人，其实三年前跟你擦肩而过了，但你当时在低头想台词。',
  },
  HWMP: {
    code: 'HWMP',
    cn: '暗恋教主',
    intro: 'The Shadow Lover',
    desc: '暗恋是你的主修专业，辅修方向是自我感动。你可以喜欢一个人三年五年甚至更久，对方完全不知情，但你觉得这份独自承担的感情特别纯洁、特别高级。你不表白不是因为怕被拒绝——好吧其实就是怕被拒绝。你把"我爱你但我不说"包装成了一种美德，但说白了，你只是用沉默来保护自己那颗脆弱到不行的心。\n\n天赋优势：隐忍能力MAX，你能把一个秘密带进坟墓；对喜欢的人观察入微，你比TA自己还了解TA；你的爱是最不带功利心的那种——因为你压根没想过要回报。\n\n隐秘代价：你已经把暗恋变成了舒适区，真的有人喜欢你了你反而不知所措；你花在脑补上的时间，够你学会三门外语了；你最心碎的时刻不是被拒绝，而是你鼓足勇气想表白的那天，发现对方已经有新欢了。\n\n一句刺痛你的话：你以为你在守护一份纯爱，其实你只是在用暗恋逃避真实关系里必须面对的一切。',
  },
  HWDR: {
    code: 'HWDR',
    cn: '猫',
    intro: 'The Cat',
    desc: '你在感情里就像一只猫——我来找你的时候你要在，但我不来的时候你别来烦我。你内心其实很柔软、很渴望被爱，但你绝对不会主动表达。你喜欢一个人的方式是在TA身边待着、偶尔蹭一蹭，但你绝不会像狗一样摇着尾巴跑过去。你需要对方有足够的耐心来读懂你那些隐晦到近乎不存在的暗示。\n\n天赋优势：极致的人格魅力——你越不在乎，别人越想靠近你；独立而不依赖，给对方极度舒适的相处体验；你给出的每一点亲密都是真实的，没有一丝表演。\n\n隐秘代价：你的冷淡把90%真心想靠近你的人劝退了；你以为对方能读懂你的暗示，对方以为你根本不在乎；你那些"偶尔的温柔"在你心里是疯狂示爱，在对方眼里约等于正常社交。\n\n一句刺痛你的话：你装了一辈子的高冷，其实你比谁都怕那个人真的不来了。',
  },
  HWDP: {
    code: 'HWDP',
    cn: '漂流瓶',
    intro: 'The Drifter',
    desc: '你对感情的态度总结起来就一个字——随。喜欢就喜欢，不喜欢就算了，在一起就好好过，分开了也不至于死。你不主动追人、不刻意维持、不寄托幻想，你就像一个漂流瓶一样在感情的海洋里随波逐流。偶尔有人捡到你打开看看，你也觉得挺好。没人捡你也无所谓，海里还有鱼看呢。\n\n天赋优势：情绪极度稳定，失恋了也不会影响第二天的工作和食欲；绝对不会爱到失去自我，你永远是你自己；给对方零压力的相处感，你是最好的树洞和避风港。\n\n隐秘代价：你的"随缘"已经进化成了"不在乎"，你自己都分不清了；你没有在任何一段感情里真正燃烧过，所以你也不知道自己到底能爱到什么程度；所有人都觉得你很好，但没有人觉得你不可替代。\n\n一句刺痛你的话：你对所有人都温柔，因为你对谁都没有真正心动过。',
  },
  EX: {
    code: 'EX',
    cn: '前任幽灵',
    intro: 'The Ghost of Ex',
    desc: '你以为你在做恋爱人格测试？不，你在做前任回忆测试。你的每一个选择背后都有一个影子。你嘴上说着"早就放下了"，但你的潜意识已经把你出卖了——你根本没走出来。你还活在上一段感情的废墟里，一边收拾碎片一边假装在重新装修。\n\n天赋优势：你比任何人都更深刻地理解爱是什么，因为你真的失去过；极强的反思能力，你对自己在感情中的问题看得一清二楚；你受过的伤让你更温柔，你不会轻易去伤害另一个人。\n\n隐秘代价：你不是在等新的人，你是在等一个不可能的"再来一次"；每个新认识的人都在跟你的前任做比较，这对他们不公平；你把"念旧"说得很好听，但说白了你只是不敢翻篇。\n\n一句刺痛你的话：你放不下的不是那个人，是那个还相信爱情的自己。',
  },
};

// Pattern: D1(A=1,B=3) D2(A=1,B=3) D3(A=1,B=3) D4(A=1,B=3)
// Using A/B instead of L/M/H — A=low pole, B=high pole
export const NORMAL_TYPES: NormalType[] = [
  { code: 'BAMR', pattern: 'BAMA' }, // B=3,A=1,M=1,R=1 → 3,1,1,1
  { code: 'BAMP', pattern: 'BAMB' }, // B=3,A=1,M=1,P=3 → 3,1,1,3
  { code: 'BADR', pattern: 'BABA' }, // B=3,A=1,D=3,R=1 → 3,1,3,1
  { code: 'BADP', pattern: 'BABB' }, // B=3,A=1,D=3,P=3 → 3,1,3,3
  { code: 'BWMR', pattern: 'BBAA' }, // B=3,W=3,M=1,R=1 → 3,3,1,1
  { code: 'BWMP', pattern: 'BBAB' }, // B=3,W=3,M=1,P=3 → 3,3,1,3
  { code: 'BWDR', pattern: 'BBBA' }, // B=3,W=3,D=3,R=1 → 3,3,3,1
  { code: 'BWDP', pattern: 'BBBB' }, // B=3,W=3,D=3,P=3 → 3,3,3,3
  { code: 'HAMR', pattern: 'AAAA' }, // H=1,A=1,M=1,R=1 → 1,1,1,1
  { code: 'HAMP', pattern: 'AAAB' }, // H=1,A=1,M=1,P=3 → 1,1,1,3
  { code: 'HADR', pattern: 'AABA' }, // H=1,A=1,D=3,R=1 → 1,1,3,1
  { code: 'HADP', pattern: 'AABB' }, // H=1,A=1,D=3,P=3 → 1,1,3,3
  { code: 'HWMR', pattern: 'ABAA' }, // H=1,W=3,M=1,R=1 → 1,3,1,1
  { code: 'HWMP', pattern: 'ABAB' }, // H=1,W=3,M=1,P=3 → 1,3,1,3
  { code: 'HWDR', pattern: 'ABBA' }, // H=1,W=3,D=3,R=1 → 1,3,3,1
  { code: 'HWDP', pattern: 'ABBB' }, // H=1,W=3,D=3,P=3 → 1,3,3,3
];

export const TYPE_RARITY: Record<string, RarityInfo> = {
  BAMR: { pct: 6.25, stars: 3, label: '稀有' },
  BAMP: { pct: 6.25, stars: 3, label: '稀有' },
  BADR: { pct: 6.25, stars: 3, label: '稀有' },
  BADP: { pct: 6.25, stars: 3, label: '稀有' },
  BWMR: { pct: 6.25, stars: 3, label: '稀有' },
  BWMP: { pct: 6.25, stars: 3, label: '稀有' },
  BWDR: { pct: 6.25, stars: 3, label: '稀有' },
  BWDP: { pct: 6.25, stars: 3, label: '稀有' },
  HAMR: { pct: 6.25, stars: 3, label: '稀有' },
  HAMP: { pct: 6.25, stars: 3, label: '稀有' },
  HADR: { pct: 6.25, stars: 3, label: '稀有' },
  HADP: { pct: 6.25, stars: 3, label: '稀有' },
  HWMR: { pct: 6.25, stars: 3, label: '稀有' },
  HWMP: { pct: 6.25, stars: 3, label: '稀有' },
  HWDR: { pct: 6.25, stars: 3, label: '稀有' },
  HWDP: { pct: 6.25, stars: 3, label: '稀有' },
  EX: { pct: 0, stars: 5, label: '隐藏' },
};
```

- [ ] **Step 4: Create love compatibility**

```typescript
// src/data/love/compatibility.ts
import type { CompatEntry, CompatResult } from '../testConfig';

export const COMPATIBILITY: Record<string, CompatEntry> = {
  // Soulmates (互补型)
  'HAMR+BADP': { type: 'soulmate', say: '飞蛾遇到审计官——一个需要被兜底，一个需要被点燃' },
  'HWMR+HAMP': { type: 'soulmate', say: '月光信徒遇到纵火犯——一个满脑子剧本，一个专门把剧本变现' },
  'BWDP+HAMR': { type: 'soulmate', say: '孤岛遇到飞蛾——孤岛终于让一个人登陆了，飞蛾终于有了不烫手的家' },
  'HADR+HWDR': { type: 'soulmate', say: '骑士遇到猫——骑士默默守护，猫偶尔施舍一个靠近' },
  'BAMR+HWMP': { type: 'soulmate', say: '猎头遇到暗恋教主——一个狂追不舍，一个终于被人看到' },
  'BWMR+HADP': { type: 'soulmate', say: '完美主义者遇到赌徒——一个要确定性，一个带来所有的不确定' },
  'BAMP+HWMR': { type: 'soulmate', say: '操盘手遇到月光信徒——一个绝对理性，一个绝对感性，彼此是解药' },
  'BADR+HWDP': { type: 'soulmate', say: '策展人遇到漂流瓶——一个精心策划，一个随遇而安，互相中和' },
  // Rivals (冲突型)
  'HAMR+HAMP': { type: 'rival', say: '飞蛾遇到纵火犯——一个越烧越旺一个烧完就跑，核弹级灾难' },
  'BWDP+HWMR': { type: 'rival', say: '孤岛遇到月光信徒——一个永远靠不近，一个永远在等，两条平行线' },
  'BADP+HADP': { type: 'rival', say: '审计官遇到赌徒——一个要风控一个要all in，能吵到天荒地老' },
  'BAMR+HWDR': { type: 'rival', say: '猎头遇到猫——猎头的攻势让猫窒息，猫的冷淡让猎头崩溃' },
  'BWMP+HAMR': { type: 'rival', say: '局外人遇到飞蛾——一个冷眼旁观一个烈火烹油，温差能冻死人' },
  'BAMP+HADR': { type: 'rival', say: '操盘手遇到骑士——一个算计一个奉献，骑士被算计到心寒' },
  'BWMR+HWDP': { type: 'rival', say: '完美主义者遇到漂流瓶——一个要求100分一个连卷子都懒得看' },
  'BADR+HAMP': { type: 'rival', say: '策展人遇到纵火犯——一个求精致一个搞破坏，审美冲突不可调和' },
};

export function getCompatibility(codeA: string, codeB: string): CompatResult {
  if (codeA === codeB) {
    return { type: 'mirror', say: '你们是同一种恋爱人格——要么惺惺相惜，要么互相照出最不想看到的自己。' };
  }
  const entry = COMPATIBILITY[`${codeA}+${codeB}`] || COMPATIBILITY[`${codeB}+${codeA}`];
  if (entry) {
    return { type: entry.type, say: entry.say };
  }
  return { type: 'normal', say: '你们之间没有特别强的化学反应，但也没有致命冲突。一切看缘分和经营。' };
}
```

- [ ] **Step 5: Create love typeImages (placeholders)**

```typescript
// src/data/love/typeImages.ts
// Placeholder — no custom images yet, components will fallback to text display
export const TYPE_IMAGES: Record<string, string> = {};
export const SHARE_IMAGES: Record<string, string> = {};
```

- [ ] **Step 6: Verify all files compile**

Run: `npx tsc --noEmit 2>&1 | head -20`
Expected: No errors

- [ ] **Step 7: Commit**

```bash
git add src/data/love/
git commit -m "feat(lq16): add love test data — dimensions, questions, types, compatibility"
```

---

### Task 3: Create SBTI and Love config instances

**Files:**
- Create: `src/data/sbti/config.ts`
- Create: `src/data/love/config.ts`

- [ ] **Step 1: Create SBTI config (wrapping existing data)**

```typescript
// src/data/sbti/config.ts
import type { TestConfig } from '../testConfig';
import { dimensionMeta, dimensionOrder, DIM_EXPLANATIONS } from '../dimensions';
import { questions, specialQuestions } from '../questions';
import { TYPE_LIBRARY, NORMAL_TYPES, TYPE_RARITY } from '../types';
import { TYPE_IMAGES, SHARE_IMAGES } from '../typeImages';
import { COMPATIBILITY, getCompatibility } from '../compatibility';

function sumToLevel(score: number): string {
  if (score <= 3) return 'L';
  if (score === 4) return 'M';
  return 'H';
}

export const sbtiConfig: TestConfig = {
  id: 'sbti',
  name: 'SBTI 人格测试',
  dimensionOrder,
  dimensionMeta,
  dimExplanations: DIM_EXPLANATIONS,
  questions,
  specialQuestions,
  gateQuestionId: 'drink_gate_q1',
  gateAnswerValue: 3,
  hiddenTriggerQuestionId: 'drink_gate_q2',
  hiddenTriggerValue: 2,
  typeLibrary: TYPE_LIBRARY,
  normalTypes: NORMAL_TYPES,
  typeRarity: TYPE_RARITY,
  typeImages: TYPE_IMAGES,
  shareImages: SHARE_IMAGES,
  compatibility: COMPATIBILITY,
  getCompatibility,
  sumToLevel,
  maxDistance: 30,
  fallbackTypeCode: 'HHHH',
  hiddenTypeCode: 'DRUNK',
  similarityThreshold: 60,
  prodBaseUrl: 'https://sbti.jiligulu.xyz',
  basePath: '/new',
  localHistoryKey: 'sbti_history',
  localStatsKey: 'sbti_local_stats',
  apiTestParam: '',
  dimSectionTitle: '十五维度评分',
  questionCountLabel: '31',
};
```

- [ ] **Step 2: Create Love config**

```typescript
// src/data/love/config.ts
import type { TestConfig } from '../testConfig';
import { dimensionMeta, dimensionOrder, DIM_EXPLANATIONS } from './dimensions';
import { questions, specialQuestions } from './questions';
import { TYPE_LIBRARY, NORMAL_TYPES, TYPE_RARITY } from './types';
import { TYPE_IMAGES, SHARE_IMAGES } from './typeImages';
import { COMPATIBILITY, getCompatibility } from './compatibility';

function sumToLevel(score: number): string {
  if (score <= 6) return 'A';
  return 'B';
}

export function levelNum(level: string): number {
  return level === 'A' ? 1 : level === 'B' ? 3 : 2;
}

export const loveConfig: TestConfig = {
  id: 'love',
  name: 'LQ16 恋爱人格矩阵',
  dimensionOrder,
  dimensionMeta,
  dimExplanations: DIM_EXPLANATIONS,
  questions,
  specialQuestions,
  gateQuestionId: 'ex_gate',
  gateAnswerValue: 3,
  hiddenTriggerQuestionId: 'ex_gate',
  hiddenTriggerValue: 3,
  typeLibrary: TYPE_LIBRARY,
  normalTypes: NORMAL_TYPES,
  typeRarity: TYPE_RARITY,
  typeImages: TYPE_IMAGES,
  shareImages: SHARE_IMAGES,
  compatibility: COMPATIBILITY,
  getCompatibility,
  sumToLevel,
  maxDistance: 8,
  fallbackTypeCode: 'HWDP',
  hiddenTypeCode: 'EX',
  similarityThreshold: 60,
  prodBaseUrl: 'https://sbti.jiligulu.xyz',
  basePath: '/new/love',
  localHistoryKey: 'love_history',
  localStatsKey: 'love_local_stats',
  apiTestParam: 'love',
  dimSectionTitle: '四维度评分',
  questionCountLabel: '16',
};
```

- [ ] **Step 3: Verify compile**

Run: `npx tsc --noEmit 2>&1 | head -20`
Expected: No errors

- [ ] **Step 4: Commit**

```bash
git add src/data/sbti/config.ts src/data/love/config.ts
git commit -m "feat(lq16): add SBTI and love TestConfig instances"
```

---

### Task 4: Generalize utility functions

**Files:**
- Modify: `src/utils/matching.ts`
- Modify: `src/utils/quiz.ts`
- Modify: `src/utils/compare.ts`

- [ ] **Step 1: Generalize matching.ts**

Replace the entire file. The key change: `computeResult` accepts a `TestConfig` param instead of importing data at top-level. The old `sumToLevel` and `levelNum` remain exported for backwards compat but the core function uses config.

```typescript
// src/utils/matching.ts
import type { TestConfig, TypeDef } from '../data/testConfig';

/* ---------- helpers ---------- */

export function sumToLevel(score: number): string {
  if (score <= 3) return 'L';
  if (score === 4) return 'M';
  return 'H';
}

export function levelNum(level: string): number {
  return ({ L: 1, M: 2, H: 3, A: 1, B: 3 } as Record<string, number>)[level] ?? 2;
}

export function parsePattern(pattern: string): string[] {
  return pattern.replace(/-/g, '').split('');
}

/* ---------- result types ---------- */

export interface RankedType extends TypeDef {
  pattern: string;
  distance: number;
  exact: number;
  similarity: number;
}

export interface ComputeResultOutput {
  rawScores: Record<string, number>;
  levels: Record<string, string>;
  ranked: RankedType[];
  bestNormal: RankedType;
  finalType: RankedType | TypeDef;
  modeKicker: string;
  badge: string;
  sub: string;
  special: boolean;
  secondaryType: RankedType | null;
}

/* ---------- main algorithm ---------- */

export function computeResult(
  answers: Record<string, number>,
  hiddenTriggered: boolean,
  config: TestConfig,
  debugForceType?: string | null,
): ComputeResultOutput {
  const { dimensionOrder, dimensionMeta, questions, typeLibrary, normalTypes, maxDistance, fallbackTypeCode, hiddenTypeCode, similarityThreshold } = config;
  const dimCount = dimensionOrder.length;

  // 1. Sum raw scores per dimension
  const rawScores: Record<string, number> = {};
  Object.keys(dimensionMeta).forEach(dim => { rawScores[dim] = 0; });

  questions.forEach(q => {
    if (q.dim) {
      rawScores[q.dim] += Number(answers[q.id] || 0);
    }
  });

  // 2. Convert to levels using config's sumToLevel
  const levels: Record<string, string> = {};
  Object.entries(rawScores).forEach(([dim, score]) => {
    levels[dim] = config.sumToLevel(score);
  });

  // 3. Build user vector
  const userVector = dimensionOrder.map(dim => levelNum(levels[dim]));

  // 4. Compare against each NORMAL_TYPE pattern
  const ranked: RankedType[] = normalTypes.map(type => {
    const vector = parsePattern(type.pattern).map(levelNum);
    let distance = 0;
    let exact = 0;
    for (let i = 0; i < vector.length; i++) {
      const diff = Math.abs(userVector[i] - vector[i]);
      distance += diff;
      if (diff === 0) exact += 1;
    }
    const similarity = Math.max(0, Math.round((1 - distance / maxDistance) * 100));
    return {
      ...type,
      ...typeLibrary[type.code],
      distance,
      exact,
      similarity,
    };
  }).sort((a, b) => {
    if (a.distance !== b.distance) return a.distance - b.distance;
    if (b.exact !== a.exact) return b.exact - a.exact;
    return b.similarity - a.similarity;
  });

  // 5. Pick best and apply special cases
  const bestNormal = ranked[0];

  let finalType: RankedType | TypeDef;
  let modeKicker = '你的主类型';
  let badge = `匹配度 ${bestNormal.similarity}% · 精准命中 ${bestNormal.exact}/${dimCount} 维`;
  let sub = '维度命中度较高，当前结果可视为你的第一人格画像。';
  let special = false;
  let secondaryType: RankedType | null = null;

  if (debugForceType && typeLibrary[debugForceType]) {
    finalType = { ...typeLibrary[debugForceType], similarity: 100, exact: dimCount, distance: 0 } as RankedType;
    modeKicker = '调试指定人格';
    badge = '调试模式 · 手动指定';
    sub = '当前结果由调试工具栏手动指定，非答题匹配。';
    special = true;
  } else if (hiddenTriggered) {
    finalType = typeLibrary[hiddenTypeCode];
    secondaryType = bestNormal;
    modeKicker = '隐藏人格已激活';
    badge = '匹配度 100% · 隐藏因子已接管';
    sub = '隐藏人格触发条件已满足，系统已跳过常规人格审判。';
    special = true;
  } else if (bestNormal.similarity < similarityThreshold) {
    finalType = typeLibrary[fallbackTypeCode];
    modeKicker = '系统强制兜底';
    badge = `标准人格库最高匹配仅 ${bestNormal.similarity}%`;
    sub = '标准人格库对你的脑回路集体罢工了，于是系统把你强制分配给了兜底人格。';
    special = true;
  } else {
    finalType = bestNormal;
  }

  return {
    rawScores,
    levels,
    ranked,
    bestNormal,
    finalType,
    modeKicker,
    badge,
    sub,
    special,
    secondaryType,
  };
}
```

- [ ] **Step 2: Generalize quiz.ts**

Replace the entire file. The key change: functions accept questions as parameters instead of importing.

```typescript
// src/utils/quiz.ts
import type { Question } from '../data/testConfig';

/** Fisher-Yates shuffle (returns new array) */
export function shuffle<T>(array: T[]): T[] {
  const arr = [...array];
  for (let i = arr.length - 1; i > 0; i--) {
    const j = Math.floor(Math.random() * (i + 1));
    [arr[i], arr[j]] = [arr[j], arr[i]];
  }
  return arr;
}

/**
 * Shuffle regular questions and insert gateQuestion at a random
 * position (never at index 0).
 */
export function buildShuffledQuestions(
  regularQuestions: Question[],
  gateQuestion?: Question,
): Question[] {
  const shuffledRegular = shuffle(regularQuestions);
  if (!gateQuestion) return shuffledRegular;
  const insertIndex = Math.floor(Math.random() * shuffledRegular.length) + 1;
  return [
    ...shuffledRegular.slice(0, insertIndex),
    gateQuestion,
    ...shuffledRegular.slice(insertIndex),
  ];
}

/**
 * Build the list of visible questions.
 * If the user chose the gate question with the trigger value,
 * splice the follow-up question right after it.
 */
export function getVisibleQuestions(
  shuffledQuestions: Question[],
  answers: Record<string, number>,
  gateQuestionId: string,
  gateAnswerValue: number,
  followUpQuestion?: Question,
): Question[] {
  const visible = [...shuffledQuestions];
  if (!followUpQuestion) return visible;
  const gateIndex = visible.findIndex(q => q.id === gateQuestionId);
  if (gateIndex !== -1 && answers[gateQuestionId] === gateAnswerValue) {
    visible.splice(gateIndex + 1, 0, followUpQuestion);
  }
  return visible;
}
```

- [ ] **Step 3: Generalize compare.ts**

Replace the entire file. The key change: functions accept dimensionOrder as parameter.

```typescript
// src/utils/compare.ts
const levelToNum: Record<string, number> = { L: 0, M: 1, H: 2, A: 0, B: 1 };
const numToLevel: Record<number, string[]> = {
  15: ['L', 'M', 'H'],  // SBTI: 3 levels
  4: ['A', 'B'],          // Love: 2 levels
};

export interface DecodedCompare {
  code: string;
  levels: Record<string, string>;
  similarity: number;
}

/**
 * Encode personality data into a base64 string for URL sharing.
 */
export function encodeCompare(
  code: string,
  levels: Record<string, string>,
  similarity: number,
  dimensionOrder: string[],
): string {
  const dimStr = dimensionOrder
    .map(d => levelToNum[levels[d]] ?? 0)
    .join('');
  return btoa(JSON.stringify({ c: code, d: dimStr, s: similarity }));
}

/**
 * Decode a base64 share string back into personality data.
 * Returns null if parsing fails.
 */
export function decodeCompare(
  b64: string,
  dimensionOrder: string[],
): DecodedCompare | null {
  try {
    const obj = JSON.parse(atob(b64));
    const levelMap = numToLevel[dimensionOrder.length] || numToLevel[15];
    const levels: Record<string, string> = {};
    dimensionOrder.forEach((d, i) => {
      levels[d] = levelMap[parseInt(obj.d[i])] ?? levelMap[0];
    });
    return { code: obj.c, levels, similarity: obj.s };
  } catch {
    return null;
  }
}
```

- [ ] **Step 4: Verify compile**

Run: `npx tsc --noEmit 2>&1 | head -30`
Expected: Errors in files that still import old signatures (App.tsx, useQuiz.ts, ComparePage.tsx) — this is expected and will be fixed in subsequent tasks.

- [ ] **Step 5: Commit**

```bash
git add src/utils/matching.ts src/utils/quiz.ts src/utils/compare.ts
git commit -m "feat(lq16): generalize utils to accept test config params"
```

---

### Task 5: Generalize hooks

**Files:**
- Modify: `src/hooks/useQuiz.ts`
- Modify: `src/hooks/useRanking.ts`
- Modify: `src/hooks/useLocalHistory.ts`

- [ ] **Step 1: Generalize useQuiz**

Replace the entire file. Key change: uses `useTestConfig()` context to get questions, matching config, gate logic.

```typescript
// src/hooks/useQuiz.ts
import { useState, useMemo, useCallback } from 'react';
import type { Question } from '../data/testConfig';
import { useTestConfig } from '../data/testConfig';
import { buildShuffledQuestions, getVisibleQuestions } from '../utils/quiz';
import { computeResult, type ComputeResultOutput } from '../utils/matching';

export interface UseQuizReturn {
  /* state */
  shuffledQuestions: Question[];
  answers: Record<string, number>;
  currentQ: number;
  previewMode: boolean;
  debugForceType: string | null;

  /* derived */
  visibleQuestions: Question[];
  totalQuestions: number;
  answeredCount: number;
  allAnswered: boolean;
  progress: number;
  currentQuestion: Question | undefined;

  /* actions */
  startQuiz: (preview?: boolean) => void;
  answer: (qId: string, value: number) => void;
  goNext: () => void;
  goPrev: () => void;
  setDebugForceType: (code: string | null) => void;
  getResult: () => ComputeResultOutput;
}

export function useQuiz(): UseQuizReturn {
  const config = useTestConfig();
  const [shuffledQuestions, setShuffledQuestions] = useState<Question[]>([]);
  const [answers, setAnswers] = useState<Record<string, number>>({});
  const [currentQ, setCurrentQ] = useState(0);
  const [previewMode, setPreviewMode] = useState(false);
  const [debugForceType, setDebugForceType] = useState<string | null>(null);

  // Love test: gate and trigger are the same question (ex_gate, value 3)
  // SBTI: gate is drink_gate_q1 (value 3), trigger is drink_gate_q2 (value 2)
  const hasFollowUp = config.gateQuestionId !== config.hiddenTriggerQuestionId;

  /* derived */
  const visibleQuestions = useMemo(
    () => getVisibleQuestions(
      shuffledQuestions,
      answers,
      config.gateQuestionId,
      config.gateAnswerValue,
      hasFollowUp ? config.specialQuestions[1] : undefined,
    ),
    [shuffledQuestions, answers, config, hasFollowUp],
  );

  const totalQuestions = visibleQuestions.length;
  const answeredCount = visibleQuestions.filter(q => answers[q.id] !== undefined).length;
  const allAnswered = answeredCount === totalQuestions && totalQuestions > 0;
  const progress = totalQuestions ? (answeredCount / totalQuestions) * 100 : 0;
  const currentQuestion = visibleQuestions[currentQ];

  const hiddenTriggered = answers[config.hiddenTriggerQuestionId] === config.hiddenTriggerValue;

  /* actions */
  const startQuiz = useCallback((preview = false) => {
    setPreviewMode(preview);
    setAnswers({});
    setCurrentQ(0);
    setDebugForceType(null);
    setShuffledQuestions(
      buildShuffledQuestions(config.questions, config.specialQuestions[0]),
    );
  }, [config]);

  const answer = useCallback((qId: string, value: number) => {
    setAnswers(prev => {
      const next = { ...prev, [qId]: value };
      // If gate question changed away from trigger value, remove follow-up answer
      if (hasFollowUp && qId === config.gateQuestionId && value !== config.gateAnswerValue) {
        delete next[config.hiddenTriggerQuestionId];
      }
      return next;
    });
  }, [config, hasFollowUp]);

  const goNext = useCallback(() => {
    setCurrentQ(prev => {
      const max = visibleQuestions.length - 1;
      return prev < max ? prev + 1 : prev;
    });
  }, [visibleQuestions.length]);

  const goPrev = useCallback(() => {
    setCurrentQ(prev => (prev > 0 ? prev - 1 : 0));
  }, []);

  const getResult = useCallback(
    (): ComputeResultOutput => computeResult(answers, hiddenTriggered, config, debugForceType),
    [answers, hiddenTriggered, config, debugForceType],
  );

  return {
    shuffledQuestions,
    answers,
    currentQ,
    previewMode,
    debugForceType,

    visibleQuestions,
    totalQuestions,
    answeredCount,
    allAnswered,
    progress,
    currentQuestion,

    startQuiz,
    answer,
    goNext,
    goPrev,
    setDebugForceType,
    getResult,
  };
}
```

- [ ] **Step 2: Generalize useRanking**

Replace the entire file. Key change: accepts `apiTestParam` to build the correct API URL.

```typescript
// src/hooks/useRanking.ts
import { useState, useCallback } from 'react';
import { useTestConfig } from '../data/testConfig';

export interface RankingItem {
  code: string;
  count: number;
}

export interface RankingData {
  list: RankingItem[];
  total: number;
}

export interface UseRankingReturn {
  data: RankingData | null;
  loading: boolean;
  error: string | null;
  fetchRanking: () => Promise<void>;
}

export function useRanking(): UseRankingReturn {
  const config = useTestConfig();
  const [data, setData] = useState<RankingData | null>(null);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<string | null>(null);

  const fetchRanking = useCallback(async () => {
    setLoading(true);
    setError(null);
    try {
      const params = config.apiTestParam ? `?test=${config.apiTestParam}` : '';
      const res = await fetch(`/api/ranking${params}`);
      if (!res.ok) throw new Error(`HTTP ${res.status}`);
      const json = await res.json();
      setData({ list: json.list ?? [], total: json.total ?? 0 });
    } catch (err) {
      setError(err instanceof Error ? err.message : 'Unknown error');
    } finally {
      setLoading(false);
    }
  }, [config.apiTestParam]);

  return { data, loading, error, fetchRanking };
}
```

- [ ] **Step 3: Generalize useLocalHistory**

Replace the entire file. Key change: uses config for localStorage keys and API path.

```typescript
// src/hooks/useLocalHistory.ts
import { useState, useEffect, useCallback } from 'react';
import { useTestConfig } from '../data/testConfig';

export interface HistoryEntry {
  code: string;
  time: number;
}

export interface UseLocalHistoryReturn {
  history: HistoryEntry[];
  stats: Record<string, number>;
  saveResult: (typeCode: string) => Promise<void>;
}

function readHistory(key: string): HistoryEntry[] {
  try {
    return JSON.parse(localStorage.getItem(key) || '[]');
  } catch {
    return [];
  }
}

function readStats(key: string): Record<string, number> {
  try {
    return JSON.parse(localStorage.getItem(key) || '{}');
  } catch {
    return {};
  }
}

export function useLocalHistory(): UseLocalHistoryReturn {
  const config = useTestConfig();
  const [history, setHistory] = useState<HistoryEntry[]>([]);
  const [stats, setStats] = useState<Record<string, number>>({});

  // Initialise from localStorage on mount
  useEffect(() => {
    setHistory(readHistory(config.localHistoryKey));
    setStats(readStats(config.localStatsKey));
  }, [config.localHistoryKey, config.localStatsKey]);

  const saveResult = useCallback(async (typeCode: string) => {
    // POST to remote (fire-and-forget)
    const body: Record<string, string> = { type: typeCode };
    if (config.apiTestParam) body.test = config.apiTestParam;
    fetch('/api/record', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify(body),
    }).catch(() => {});

    // Update stats
    const nextStats = { ...readStats(config.localStatsKey) };
    nextStats[typeCode] = (nextStats[typeCode] || 0) + 1;
    localStorage.setItem(config.localStatsKey, JSON.stringify(nextStats));
    setStats(nextStats);

    // Update history (keep last 100)
    let nextHistory = [...readHistory(config.localHistoryKey), { code: typeCode, time: Date.now() }];
    if (nextHistory.length > 100) nextHistory = nextHistory.slice(-100);
    localStorage.setItem(config.localHistoryKey, JSON.stringify(nextHistory));
    setHistory(nextHistory);
  }, [config]);

  return { history, stats, saveResult };
}
```

- [ ] **Step 4: Verify compile**

Run: `npx tsc --noEmit 2>&1 | head -30`
Expected: Errors only in App.tsx and components that still import old data directly — fixed next.

- [ ] **Step 5: Commit**

```bash
git add src/hooks/useQuiz.ts src/hooks/useRanking.ts src/hooks/useLocalHistory.ts
git commit -m "feat(lq16): generalize hooks to use TestConfig context"
```

---

### Task 6: Generalize display components

**Files:**
- Modify: `src/components/DimList.tsx`
- Modify: `src/components/ResultPage.tsx`
- Modify: `src/components/ComparePage.tsx`

- [ ] **Step 1: Generalize DimList**

Replace the file. Key change: reads dimension data from `useTestConfig()` instead of importing.

```typescript
// src/components/DimList.tsx
import { useTestConfig } from '../data/testConfig';

interface DimListProps {
  levels: Record<string, string>;
  rawScores: Record<string, number>;
}

const levelWidth: Record<string, string> = {
  L: '33%', M: '66%', H: '100%',
  A: '33%', B: '100%',
};

const levelColor: Record<string, string> = {
  L: '#ff3b3b', M: '#ffaa00', H: '#44ff88',
  A: '#ff3b3b', B: '#44ff88',
};

export default function DimList({ levels, rawScores }: DimListProps) {
  const config = useTestConfig();

  return (
    <div>
      {config.dimensionOrder.map((dim, i) => {
        const level = levels[dim] ?? 'M';
        const meta = config.dimensionMeta[dim];
        const shortName = meta.name.replace(/^[A-Za-z]+\d*\s*/, '');
        const color = levelColor[level] ?? '#ffaa00';
        const width = levelWidth[level] ?? '66%';
        const explanation = config.dimExplanations[dim]?.[level as string];

        return (
          <div
            key={dim}
            className={`flex items-center gap-4 py-3 ${
              i < config.dimensionOrder.length - 1 ? 'border-b border-[#1a1a1a]' : ''
            }`}
            title={explanation}
          >
            <span className="text-xs text-[#888] w-[100px] flex-shrink-0">
              {shortName}
            </span>
            <div className="flex-1 h-1.5 bg-[#1a1a1a] rounded-full overflow-hidden">
              <div
                className="h-full rounded-full transition-all duration-500"
                style={{
                  width,
                  backgroundColor: color,
                }}
              />
            </div>
            <span
              className="font-mono text-xs font-bold w-6 text-center"
              style={{ color }}
            >
              {level}
            </span>
          </div>
        );
      })}
    </div>
  );
}
```

- [ ] **Step 2: Generalize ResultPage**

Replace the data imports. Change these imports at top of `src/components/ResultPage.tsx`:

Old:
```typescript
import { TYPE_IMAGES } from '../data/typeImages';
import { TYPE_LIBRARY } from '../data/types';
import { COMPATIBILITY } from '../data/compatibility';
```

New:
```typescript
import { useTestConfig } from '../data/testConfig';
```

Then inside the component function, add at the top:
```typescript
const config = useTestConfig();
```

And replace all references:
- `TYPE_LIBRARY[typeCode]` → `config.typeLibrary[typeCode]`
- `TYPE_IMAGES[typeCode]` → `config.typeImages[typeCode]`
- `COMPATIBILITY` → `config.compatibility`
- `TYPE_LIBRARY[otherCode]` → `config.typeLibrary[otherCode]`
- The debug select `Object.entries(TYPE_LIBRARY)` → `Object.entries(config.typeLibrary)`
- The section title `十五维度评分` → `{config.dimSectionTitle}`

- [ ] **Step 3: Generalize ComparePage**

Replace the data imports in `src/components/ComparePage.tsx`:

Old:
```typescript
import { TYPE_IMAGES } from '../data/typeImages';
import { TYPE_LIBRARY } from '../data/types';
import { dimensionOrder, dimensionMeta } from '../data/dimensions';
import { getCompatibility } from '../data/compatibility';
```

New:
```typescript
import { useTestConfig } from '../data/testConfig';
```

Inside the component, add at the top:
```typescript
const config = useTestConfig();
```

Replace all references:
- `TYPE_LIBRARY[code]` → `config.typeLibrary[code]`
- `TYPE_IMAGES[code]` → `config.typeImages[code]`
- `dimensionOrder` → `config.dimensionOrder`
- `dimensionMeta[d]` → `config.dimensionMeta[d]`
- `getCompatibility(...)` → `config.getCompatibility(...)`

- [ ] **Step 4: Verify compile**

Run: `npx tsc --noEmit 2>&1 | head -30`
Expected: Errors only in App.tsx (still needs wrapping) — fixed next.

- [ ] **Step 5: Commit**

```bash
git add src/components/DimList.tsx src/components/ResultPage.tsx src/components/ComparePage.tsx
git commit -m "feat(lq16): generalize display components to use TestConfig context"
```

---

### Task 7: Wrap SBTI App with config context

**Files:**
- Modify: `src/App.tsx`

- [ ] **Step 1: Update App.tsx imports and wrap with provider**

At the top of `src/App.tsx`, replace the data imports:

Old:
```typescript
import { TYPE_LIBRARY } from './data/types';
import { PROD_BASE_URL } from './theme/tokens';
import { computeResult, type ComputeResultOutput } from './utils/matching';
import { questions, specialQuestions } from './data/questions';
```

New:
```typescript
import { TestConfigProvider, useTestConfig } from './data/testConfig';
import { sbtiConfig } from './data/sbti/config';
import { PROD_BASE_URL } from './theme/tokens';
import { computeResult, type ComputeResultOutput } from './utils/matching';
```

Also update the compare import:
Old:
```typescript
import { encodeCompare, decodeCompare, type DecodedCompare } from './utils/compare';
```

New (same import, usage changes below).

- [ ] **Step 2: Refactor App function to use config**

Rename the current `App` function to `AppInner`, add `useTestConfig()` at the top, and replace all hardcoded data references:

```typescript
function AppInner() {
  const config = useTestConfig();
  // ... existing state ...

  const autoFillAndShowResult = useCallback(() => {
    const allQs = [...config.questions, ...config.specialQuestions];
    const answers: Record<string, number> = {};
    allQs.forEach((q) => {
      const maxVal = q.options[q.options.length - 1].value;
      answers[q.id] = Math.floor(Math.random() * maxVal) + 1;
    });
    const res = computeResult(answers, false, config, null);
    setResult(res);
    setScreen('result');
  }, [config]);

  // Hash routing — use config.basePath for compare URLs
  useEffect(() => {
    const hash = window.location.hash;
    if (hash === '#test' || isTestDomain) {
      autoFillAndShowResult();
    } else if (hash.startsWith('#compare=')) {
      const b64 = hash.slice('#compare='.length);
      const decoded = decodeCompare(b64, config.dimensionOrder);
      if (decoded) {
        setCompareData(decoded);
        setScreen('compare');
      }
    }
  }, []); // eslint-disable-line react-hooks/exhaustive-deps

  const handleQuizSubmit = useCallback(() => {
    const res = quiz.getResult();
    setResult(res);
    localHistory.saveResult(res.finalType.code);
    setScreen('interstitial');
  }, [quiz, localHistory]);

  const handleShare = useCallback(async () => {
    if (!result) return;
    const typeCode = result.finalType.code;
    const typeDef = config.typeLibrary[typeCode] ?? result.finalType;
    const pageUrl = config.prodBaseUrl + config.basePath;
    const qrDataUrl = generateQR(pageUrl);
    try {
      const canvas = await drawShareCard(typeDef, result, qrDataUrl, 'share');
      const blob = await canvasToBlob(canvas);
      setShareModalBlob(blob);
      setShareModalFileName(`${config.id}-${typeCode}.png`);
      setShareModalUrl(pageUrl);
      setShowShareModal(true);
    } catch {
      alert('分享图生成失败');
    }
  }, [result, config]);

  const handleInviteCompare = useCallback(async () => {
    if (!result) return;
    const typeCode = result.finalType.code;
    const typeDef = config.typeLibrary[typeCode] ?? result.finalType;
    const similarity = 'similarity' in result.finalType
      ? (result.finalType as { similarity: number }).similarity
      : 0;
    const encoded = encodeCompare(
      result.finalType.code,
      result.levels,
      similarity,
      config.dimensionOrder,
    );
    const compareUrl = `${config.prodBaseUrl}${config.basePath}#compare=${encoded}`;
    const qrDataUrl = generateQR(compareUrl);
    try {
      const canvas = await drawShareCard(typeDef, result, qrDataUrl, 'invite');
      const blob = await canvasToBlob(canvas);
      setShareModalBlob(blob);
      setShareModalFileName(`${config.id}-invite-${typeCode}.png`);
      setShareModalUrl(compareUrl);
      setShowShareModal(true);
    } catch {
      if (navigator.clipboard) {
        navigator.clipboard.writeText(compareUrl).then(() => {
          alert('对比链接已复制到剪贴板！发给好友即可对比人格。');
        }).catch(() => {
          prompt('复制以下链接发给好友：', compareUrl);
        });
      } else {
        prompt('复制以下链接发给好友：', compareUrl);
      }
    }
  }, [result, config]);

  const handleDebugForceType = useCallback((code: string) => {
    const allQs = [...config.questions, ...config.specialQuestions];
    const answers: Record<string, number> = {};
    allQs.forEach((q) => {
      const maxVal = q.options[q.options.length - 1].value;
      answers[q.id] = Math.floor(Math.random() * maxVal) + 1;
    });
    const res = computeResult(answers, false, config, code);
    setResult(res);
    setScreen('result');
  }, [config]);

  const handleShareCompare = useCallback(async () => {
    if (!result || !compareData) return;
    const typeCode = result.finalType.code;
    const typeDef = config.typeLibrary[typeCode] ?? result.finalType;
    const similarity = 'similarity' in result.finalType
      ? (result.finalType as { similarity: number }).similarity
      : 0;
    const encoded = encodeCompare(
      result.finalType.code,
      result.levels,
      similarity,
      config.dimensionOrder,
    );
    const compareUrl = `${config.prodBaseUrl}${config.basePath}#compare=${encoded}`;
    const qrDataUrl = generateQR(compareUrl);
    try {
      const canvas = await drawShareCard(typeDef, result, qrDataUrl, 'invite');
      const blob = await canvasToBlob(canvas);
      setShareModalBlob(blob);
      setShareModalFileName(`${config.id}-compare-${typeCode}.png`);
      setShareModalUrl(compareUrl);
      setShowShareModal(true);
    } catch {
      alert('分享图生成失败');
    }
  }, [result, compareData, config]);

  // ... rest of render unchanged, except compare prompt uses config.typeLibrary ...
}

export default function App() {
  return (
    <TestConfigProvider config={sbtiConfig}>
      <AppInner />
    </TestConfigProvider>
  );
}
```

Also in the compare prompt section, replace `TYPE_LIBRARY[compareData.code]` with access through config:
```tsx
{config.typeLibrary[compareData.code]?.cn || ''}
```

- [ ] **Step 3: Verify compile and dev server**

Run: `npx tsc --noEmit 2>&1 | head -20`
Expected: No errors (or only unrelated warnings).

Run: `npx vite dev &` and open `http://localhost:5173/new/` to verify SBTI still works.

- [ ] **Step 4: Commit**

```bash
git add src/App.tsx
git commit -m "feat(lq16): wrap SBTI App with TestConfigProvider — no behavior change"
```

---

### Task 8: Create LoveApp entry point and routing

**Files:**
- Create: `src/LoveApp.tsx`
- Create: `love.html`
- Modify: `src/main.tsx`
- Modify: `vite.config.ts`
- Modify: `build.sh`

- [ ] **Step 1: Create LoveApp.tsx**

This is a simplified version of App.tsx that wraps with `loveConfig`. For MVP, it reuses the same component structure but with love data. The home screen is simplified (no ProfilesGallery/CompatTable tabs initially — just Hero + quiz).

```typescript
// src/LoveApp.tsx
import { useState, useEffect, useCallback } from 'react';
import { TestConfigProvider, useTestConfig } from './data/testConfig';
import { loveConfig } from './data/love/config';
import Nav, { type TabId } from './components/Nav';
import QuizOverlay from './components/QuizOverlay';
import Interstitial from './components/Interstitial';
import ResultPage from './components/ResultPage';
import ComparePage from './components/ComparePage';
import ShareModal from './components/ShareModal';
import RankingPage from './components/RankingPage';
import { useQuiz } from './hooks/useQuiz';
import { useRanking } from './hooks/useRanking';
import { useLocalHistory } from './hooks/useLocalHistory';
import { encodeCompare, decodeCompare, type DecodedCompare } from './utils/compare';
import { generateQR } from './utils/qr';
import { drawShareCard, canvasToBlob } from './utils/shareCard';
import { computeResult, type ComputeResultOutput } from './utils/matching';

type ScreenId = 'home' | 'quiz' | 'interstitial' | 'result' | 'compare';

const isTestDomain = window.location.hostname.includes('sbticc-test');

function LoveAppInner() {
  const config = useTestConfig();
  const [activeTab, setActiveTab] = useState<TabId>('home');
  const [screen, setScreen] = useState<ScreenId>('home');
  const [result, setResult] = useState<ComputeResultOutput | null>(null);
  const [compareData, setCompareData] = useState<DecodedCompare | null>(null);
  const [shareModalBlob, setShareModalBlob] = useState<Blob | null>(null);
  const [shareModalFileName, setShareModalFileName] = useState('love-share.png');
  const [shareModalUrl, setShareModalUrl] = useState('');
  const [showShareModal, setShowShareModal] = useState(false);

  const quiz = useQuiz();
  const ranking = useRanking();
  const localHistory = useLocalHistory();

  useEffect(() => { ranking.fetchRanking(); }, []); // eslint-disable-line react-hooks/exhaustive-deps

  const autoFillAndShowResult = useCallback(() => {
    const allQs = [...config.questions, ...config.specialQuestions];
    const answers: Record<string, number> = {};
    allQs.forEach((q) => {
      const maxVal = q.options[q.options.length - 1].value;
      answers[q.id] = Math.floor(Math.random() * maxVal) + 1;
    });
    const res = computeResult(answers, false, config, null);
    setResult(res);
    setScreen('result');
  }, [config]);

  useEffect(() => {
    const hash = window.location.hash;
    if (hash === '#test' || isTestDomain) {
      autoFillAndShowResult();
    } else if (hash.startsWith('#compare=')) {
      const b64 = hash.slice('#compare='.length);
      const decoded = decodeCompare(b64, config.dimensionOrder);
      if (decoded) { setCompareData(decoded); setScreen('compare'); }
    }
  }, []); // eslint-disable-line react-hooks/exhaustive-deps

  const handleStartTest = useCallback(() => { quiz.startQuiz(); setScreen('quiz'); }, [quiz]);
  const handleQuizSubmit = useCallback(() => {
    const res = quiz.getResult();
    setResult(res);
    localHistory.saveResult(res.finalType.code);
    setScreen('interstitial');
  }, [quiz, localHistory]);
  const handleInterstitialComplete = useCallback(() => {
    setScreen(compareData ? 'compare' : 'result');
  }, [compareData]);
  const handleBackToHome = useCallback(() => {
    setScreen('home'); setResult(null); setCompareData(null); window.location.hash = '';
  }, []);
  const handleRestart = useCallback(() => { quiz.startQuiz(); setResult(null); setScreen('quiz'); }, [quiz]);

  const handleShare = useCallback(async () => {
    if (!result) return;
    const typeCode = result.finalType.code;
    const typeDef = config.typeLibrary[typeCode] ?? result.finalType;
    const pageUrl = config.prodBaseUrl + config.basePath;
    const qrDataUrl = generateQR(pageUrl);
    try {
      const canvas = await drawShareCard(typeDef, result, qrDataUrl, 'share');
      const blob = await canvasToBlob(canvas);
      setShareModalBlob(blob); setShareModalFileName(`love-${typeCode}.png`);
      setShareModalUrl(pageUrl); setShowShareModal(true);
    } catch { alert('分享图生成失败'); }
  }, [result, config]);

  const handleInviteCompare = useCallback(async () => {
    if (!result) return;
    const typeCode = result.finalType.code;
    const typeDef = config.typeLibrary[typeCode] ?? result.finalType;
    const similarity = 'similarity' in result.finalType ? (result.finalType as { similarity: number }).similarity : 0;
    const encoded = encodeCompare(result.finalType.code, result.levels, similarity, config.dimensionOrder);
    const compareUrl = `${config.prodBaseUrl}${config.basePath}#compare=${encoded}`;
    const qrDataUrl = generateQR(compareUrl);
    try {
      const canvas = await drawShareCard(typeDef, result, qrDataUrl, 'invite');
      const blob = await canvasToBlob(canvas);
      setShareModalBlob(blob); setShareModalFileName(`love-invite-${typeCode}.png`);
      setShareModalUrl(compareUrl); setShowShareModal(true);
    } catch {
      if (navigator.clipboard) {
        navigator.clipboard.writeText(compareUrl).then(() => alert('对比链接已复制！')).catch(() => prompt('复制链接：', compareUrl));
      } else { prompt('复制链接：', compareUrl); }
    }
  }, [result, config]);

  const handleDebugReroll = useCallback(() => { autoFillAndShowResult(); }, [autoFillAndShowResult]);
  const handleDebugForceType = useCallback((code: string) => {
    const allQs = [...config.questions, ...config.specialQuestions];
    const answers: Record<string, number> = {};
    allQs.forEach((q) => { answers[q.id] = Math.floor(Math.random() * q.options[q.options.length - 1].value) + 1; });
    const res = computeResult(answers, false, config, code);
    setResult(res); setScreen('result');
  }, [config]);

  const handleShareCompare = useCallback(async () => {
    if (!result || !compareData) return;
    const typeCode = result.finalType.code;
    const typeDef = config.typeLibrary[typeCode] ?? result.finalType;
    const similarity = 'similarity' in result.finalType ? (result.finalType as { similarity: number }).similarity : 0;
    const encoded = encodeCompare(result.finalType.code, result.levels, similarity, config.dimensionOrder);
    const compareUrl = `${config.prodBaseUrl}${config.basePath}#compare=${encoded}`;
    const qrDataUrl = generateQR(compareUrl);
    try {
      const canvas = await drawShareCard(typeDef, result, qrDataUrl, 'invite');
      const blob = await canvasToBlob(canvas);
      setShareModalBlob(blob); setShareModalFileName(`love-compare-${typeCode}.png`);
      setShareModalUrl(compareUrl); setShowShareModal(true);
    } catch { alert('分享图生成失败'); }
  }, [result, compareData, config]);

  const totalTests = ranking.data?.total ?? 0;
  const showOverlay = screen !== 'home';

  return (
    <div className="min-h-screen bg-bg text-white font-sans">
      {!showOverlay && (
        <Nav activeTab={activeTab} onTabChange={setActiveTab} onStartTest={handleStartTest} />
      )}
      {!showOverlay && (
        <main>
          {activeTab === 'home' && (
            <div className="pt-20 flex flex-col items-center justify-center min-h-[80vh] px-4 text-center">
              <p className="text-accent font-mono text-sm tracking-widest mb-4">LQ16 LOVE QUIZ</p>
              <h1 className="text-4xl sm:text-5xl font-extrabold text-white mb-4">恋爱人格矩阵</h1>
              <p className="text-[#999] text-sm mb-2 max-w-md">4个维度 × 16种恋爱人格 — 比你自己更懂你怎么爱</p>
              {totalTests > 0 && (
                <p className="text-xs text-muted mb-6">已有 {totalTests.toLocaleString()} 人测过</p>
              )}
              <button
                onClick={handleStartTest}
                className="bg-accent text-white font-bold py-4 px-10 rounded-2xl text-lg hover:bg-red-600 transition-colors cursor-pointer"
              >
                开始测试
              </button>
              <a
                href="/new"
                className="mt-6 text-xs text-muted underline hover:text-white transition-colors"
              >
                ← 回到 SBTI 人格测试
              </a>
            </div>
          )}
          {activeTab === 'ranking' && (
            <RankingPage ranking={ranking} localHistory={localHistory} onStartTest={handleStartTest} />
          )}
        </main>
      )}
      {screen === 'quiz' && <QuizOverlay quiz={quiz} onSubmit={handleQuizSubmit} onBack={handleBackToHome} />}
      {screen === 'interstitial' && <Interstitial onComplete={handleInterstitialComplete} />}
      {screen === 'result' && result && (
        <ResultPage result={result} onShare={handleShare} onInviteCompare={handleInviteCompare}
          onRestart={handleRestart} onHome={handleBackToHome}
          onDebugReroll={handleDebugReroll} onDebugForceType={handleDebugForceType} />
      )}
      {showShareModal && (
        <ShareModal imageBlob={shareModalBlob} fileName={shareModalFileName}
          shareUrl={shareModalUrl} onClose={() => setShowShareModal(false)} />
      )}
      {screen === 'compare' && compareData && result && (
        <ComparePage myData={{ code: result.finalType.code, levels: result.levels,
          similarity: 'similarity' in result.finalType ? (result.finalType as { similarity: number }).similarity : 0 }}
          theirData={compareData} onStartTest={handleRestart} onShareCompare={handleShareCompare} />
      )}
      {screen === 'compare' && compareData && !result && (
        <div className="fixed inset-0 z-[200] bg-bg flex flex-col items-center justify-center px-4">
          <p className="text-lg text-muted mb-2">对方恋爱人格：</p>
          <p className="text-white font-mono font-bold text-3xl mb-1">{compareData.code}</p>
          <p className="text-sm text-muted mb-6">{config.typeLibrary[compareData.code]?.cn || ''}</p>
          <p className="text-sm text-[#999] mb-6">先完成测试，才能查看你们的恋爱人格对比</p>
          <button onClick={handleStartTest} className="bg-accent text-white font-bold py-3 px-8 rounded-xl hover:bg-red-600 transition-colors cursor-pointer">开始测试</button>
          <button onClick={handleBackToHome} className="mt-3 text-muted underline text-sm cursor-pointer">返回首页</button>
        </div>
      )}
    </div>
  );
}

export default function LoveApp() {
  return (
    <TestConfigProvider config={loveConfig}>
      <LoveAppInner />
    </TestConfigProvider>
  );
}
```

- [ ] **Step 2: Create love.html**

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>LQ16 恋爱人格矩阵</title>
</head>
<body>
  <div id="root"></div>
  <script type="module">
    import React from 'react'
    import ReactDOM from 'react-dom/client'
    import LoveApp from './src/LoveApp.tsx'
    import './src/index.css'

    ReactDOM.createRoot(document.getElementById('root')).render(
      React.createElement(React.StrictMode, null,
        React.createElement(LoveApp)
      )
    )
  </script>
</body>
</html>
```

- [ ] **Step 3: Update vite.config.ts for multi-entry**

```typescript
// vite.config.ts
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

export default defineConfig({
  plugins: [react({
    jsxImportSource: '@emotion/react',
  })],
  base: '/new/',
  build: {
    rollupOptions: {
      input: {
        main: 'new.html',
        love: 'love.html',
      },
    },
  },
  server: {
    proxy: {
      '/api': 'http://localhost:3000',
    },
  },
})
```

- [ ] **Step 4: Update build.sh**

```bash
#!/bin/bash
set -e

# 1. Type-check
npx tsc --noEmit

# 2. Build React app to temp directory
npx vite build --outDir dist-temp

# 3. Prepare final dist directory
rm -rf dist
mkdir -p dist/new dist/new/love

# 4. Copy React build into /new/
cp dist-temp/new.html dist/new/index.html
cp -r dist-temp/assets dist/new/assets

# 5. Copy Love build into /new/love/
cp dist-temp/love.html dist/new/love/index.html

# 6. Copy old version to dist root
cp index.html dist/index.html
cp main.js dist/main.js
cp main.css dist/main.css

# 7. Copy shared static assets
cp -r images dist/images
test -f sw.js && cp sw.js dist/sw.js

# 8. Cleanup
rm -rf dist-temp

echo "Build complete: old at /, SBTI at /new/, Love at /new/love/"
```

- [ ] **Step 5: Verify dev server**

Run: `npx vite dev`
- Open `http://localhost:5173/new/` → SBTI should still work
- Open `http://localhost:5173/love.html` → Love test home should appear

- [ ] **Step 6: Commit**

```bash
git add src/LoveApp.tsx love.html vite.config.ts build.sh
git commit -m "feat(lq16): add LoveApp entry point and multi-entry build config"
```

---

### Task 9: Update API layer for multi-test support

**Files:**
- Modify: `api/record.js`
- Modify: `api/ranking.js`

- [ ] **Step 1: Update record.js**

Add love type codes to valid types, and namespace Redis keys by test param.

```javascript
// api/record.js
import { Redis } from '@upstash/redis';

const redis = new Redis({
  url: process.env.KV_REST_API_URL,
  token: process.env.KV_REST_API_TOKEN,
});

const VALID_TYPES_BY_TEST = {
  '': new Set([
    'CTRL','OJBK','THAN-K','FAKE','SEXY','MALO','Dior-s','MUM','ZZZZ',
    'LOVE-R','IMSB','SOLO','FUCK','GOGO','JOKE-R','OH-NO','MONK','SHIT',
    'DEAD','ATM-er','THIN-K','WOC!','IMFW','POOR','BOSS','HHHH','DRUNK'
  ]),
  'love': new Set([
    'BAMR','BAMP','BADR','BADP','BWMR','BWMP','BWDR','BWDP',
    'HAMR','HAMP','HADR','HADP','HWMR','HWMP','HWDR','HWDP','EX'
  ]),
};

export default async function handler(req, res) {
  if (req.method !== 'POST') {
    return res.status(405).json({ error: 'Method not allowed' });
  }

  const { type, test = '' } = req.body || {};
  const validTypes = VALID_TYPES_BY_TEST[test] || VALID_TYPES_BY_TEST[''];
  if (!type || !validTypes.has(type)) {
    return res.status(400).json({ error: 'Invalid type' });
  }

  const prefix = test ? `${test}:` : 'sbti:';

  const ip = (req.headers['x-forwarded-for'] || '').split(',')[0].trim() || 'unknown';
  const ua = req.headers['user-agent'] || '';
  const deviceId = Buffer.from(ip + '|' + ua.substring(0, 50)).toString('base64').substring(0, 32);

  const pipeline = redis.pipeline();
  pipeline.zincrby(`${prefix}ranking`, 1, type);
  pipeline.incr(`${prefix}total`);
  pipeline.sadd(`${prefix}devices`, deviceId);
  pipeline.hset(`${prefix}device_results`, {
    [deviceId]: JSON.stringify({ type, time: Date.now() })
  });

  const results = await pipeline.exec();

  res.status(200).json({ ok: true, total: results[1] });
}
```

- [ ] **Step 2: Update ranking.js**

```javascript
// api/ranking.js
import { Redis } from '@upstash/redis';

const redis = new Redis({
  url: process.env.KV_REST_API_URL,
  token: process.env.KV_REST_API_TOKEN,
});

export default async function handler(req, res) {
  if (req.method !== 'GET') {
    return res.status(405).json({ error: 'Method not allowed' });
  }

  const realOnly = req.query.real === '1';
  const test = req.query.test || '';
  const prefix = test ? `${test}:` : 'sbti:';

  const [data, total, mockTotal, mockData] = await Promise.all([
    redis.zrange(`${prefix}ranking`, 0, -1, { rev: true, withScores: true }),
    redis.get(`${prefix}total`),
    redis.get(`${prefix}mock_total`),
    redis.hgetall(`${prefix}mock`),
  ]);

  const mock = mockData || {};
  const mockTotalNum = Number(mockTotal) || 0;

  const list = [];
  for (let i = 0; i < data.length; i += 2) {
    const code = data[i];
    const rawCount = Number(data[i + 1]);
    const mockCount = Number(mock[code]) || 0;
    const count = realOnly ? Math.max(0, rawCount - mockCount) : rawCount;
    if (count > 0 || !realOnly) {
      list.push({ code, count });
    }
  }

  if (realOnly) {
    list.sort((a, b) => b.count - a.count);
  }

  const finalTotal = realOnly ? Math.max(0, Number(total) - mockTotalNum) : Number(total);

  res.setHeader('Cache-Control', 's-maxage=10, stale-while-revalidate=30');
  res.status(200).json({ total: finalTotal, list, hasMock: mockTotalNum > 0, mockTotal: mockTotalNum });
}
```

- [ ] **Step 3: Commit**

```bash
git add api/record.js api/ranking.js
git commit -m "feat(lq16): update API to support multi-test Redis key namespacing"
```

---

### Task 10: Final integration verification

- [ ] **Step 1: Full type-check**

Run: `npx tsc --noEmit`
Expected: No errors. Fix any remaining type errors.

- [ ] **Step 2: Full build**

Run: `bash build.sh`
Expected: Build completes successfully, outputs both SBTI and love apps.

- [ ] **Step 3: Manual smoke test — SBTI**

Run dev server and verify:
- Home page loads at `/new/`
- Quiz starts and runs through all 31 questions
- Result shows with 15 dimensions
- Share and compare work

- [ ] **Step 4: Manual smoke test — Love test**

Run dev server and verify:
- Love home page loads at `/love.html` (dev) or `/new/love/` (build)
- Quiz starts and runs through 16 questions + possible hidden question
- Result shows with 4 dimensions
- Share and compare work
- Debug mode (`#test`) auto-fills and shows result

- [ ] **Step 5: Final commit**

```bash
git add -A
git commit -m "feat(lq16): complete LQ16 love personality test integration"
```
