/**
 * CaTI 暂无真实图片 — typeImages 保留为空对象。
 * TypeCard 检测到 imgSrc 为 undefined 时会自动走 CSS 生成卡片分支，
 * shareCard / ResultPage 检测到 undefined/空串时会优雅降级。
 */

export const TYPE_IMAGES: Record<string, string> = {};

export const SHARE_IMAGES: Record<string, string> = {};
